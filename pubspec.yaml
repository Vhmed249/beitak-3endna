name: beitak_3endna

description: Sudan Real Estate Platform

publish_to: none

version: 4.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  provider: ^6.1.2
  path_provider: ^2.1.0

  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.3
  firebase_messaging: ^15.1.3

  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1
  flutter_image_compress: ^2.3.0
  geolocator: ^13.0.1
  url_launcher: ^6.3.1
  package_info_plus: ^8.1.0
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  photo_view: ^0.15.0
  shimmer: ^3.0.0
  share_plus: ^10.0.2
  flutter_rating_bar: ^4.0.1
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
